import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  title: {
    fontSize: 30,
    fontWeight: 'bold',
    textAlign: 'center',
    color: '#dffaed'
  },
  logo: {
    resizeMode: 'contain',
    height: 140,
    alignItems: 'center',
    justifyContent: 'center',
    marginBottom: 15,
    marginTop: 15
  },
});